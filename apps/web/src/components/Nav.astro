---
import ThemeToggle from "./ThemeToggle.tsx";
import { VERSION, GITHUB } from "../site";

const links = [
  { href: "/docs/introduction", label: "Docs" },
  { href: "/roadmap", label: "Roadmap" },
  { href: "/docs/native-modules/overview", label: "Native Modules" },
  { href: GITHUB, label: "GitHub", external: true },
];
---

<header id="site-nav" class="sticky top-0 z-50 border-b border-transparent backdrop-blur transition-colors">
  <div class="wrap flex h-16 items-center justify-between">
    <div class="flex items-baseline gap-2">
      <a href="/" class="text-base font-semibold tracking-tight" aria-label="Bunmaska home">Bunmaska</a>
      <a
        href="/docs/changelog"
        class="tag font-mono transition-colors hover:text-text"
        style="height:1.35rem;"
        title="Changelog"
      >
        v{VERSION}
      </a>
    </div>

    <nav class="flex items-center gap-1 sm:gap-2">
      {
        links.map((l) => (
          <a
            href={l.href}
            class="rounded-sm px-3 py-2 text-sm text-text-muted transition-colors hover:text-text"
            {...(l.external ? { target: "_blank", rel: "noopener" } : {})}
          >
            {l.label}
          </a>
        ))
      }
      <span class="ml-1">
        <ThemeToggle client:load />
      </span>
    </nav>
  </div>
</header>

<style>
  #site-nav {
    background: color-mix(in srgb, var(--bg) 78%, transparent);
  }
  #site-nav.scrolled {
    border-bottom-color: var(--border);
  }
</style>

<script>
  const nav = document.getElementById("site-nav");
  const onScroll = () => {
    if (!nav) return;
    nav.classList.toggle("scrolled", window.scrollY > 8);
  };
  onScroll();
  window.addEventListener("scroll", onScroll, { passive: true });
</script>
